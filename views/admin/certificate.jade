extends ../layout

include ../includes/admin-mixins

append content
 
  .main  

append scripts
  script 
    define('editview', 
      [ 'certificates/certificateview', 'certificates/certificatemodel' ], 
      function(CertificateView, CertificateModel) {
       
            
        // Create view
        var view = new CertificateView({
          model: new CertificateModel(!{JSON.stringify(certificate)}),
          instructor : !{JSON.stringify(instructor)},
          course : !{JSON.stringify(course)},
          edition : !{JSON.stringify(edition)},
          el: $('.main')
        });

        // render it
        view.render();
      });

   