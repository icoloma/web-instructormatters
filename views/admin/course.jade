extends ../layout

include ../includes/admin-mixins

append content

  h1 Course info
  form(method="post", action="#" )
    .control-group
      label Name
      input(type="text", name="name", value=course.name, readonly="true")
    .control-group
      label Duration
      input(type="text", name="duration", value=course.duration,  readonly="true")

    .form-actions
      a(href="/courses").btn Cancel

  //form(method="post", action="/courses/" + course._id + "/delete")
  //    button.btn.btn-warning.btn-large.pull-right(type="submit") Delete