extends ../layout

include ../includes/admin-mixins

append content
   
  ul
    -courses.forEach(function (course) {
      li
        h3
          a(href="/admin/courses/" + course._id ) !{course.name}
        p !{courses.duration}
    -})



 
  a(href="/admin/courses/new").btn.btn-info New Course