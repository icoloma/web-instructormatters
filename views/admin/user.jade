extends ../layout

include ../includes/admin-mixins

append content
 
  .main  

append scripts
  script 
    define('editview', 
      [ 'users/userview', 'users/usermodel' ], 
      function(UserView, UserModel) {
       
        // Create view
        var view = new UserView({
          model: new UserModel(!{JSON.stringify(user)}),
          courses: !{JSON.stringify(courses)},
          el: $('.main')
        });

        // render it
        view.render();
      });